"use strict";
{
    const person = {
        name: "Emon",
        age: 22,
        isMarried: false,
    };
    const userName = "Joe";
    const balance = 10000;
    const isAdmin = true;
    const add = (x, y) => {
        return x + y;
    };
}
